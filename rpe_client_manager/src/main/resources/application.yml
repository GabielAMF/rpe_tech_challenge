server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: rpe-client-manager

  # All services share the same database.
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:rpe}
    username: ${DB_USER:app}
    password: ${DB_PASSWORD:app}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

  # Each service keeps its own migration history table, so migrations from
  # different services don't collide in the shared database.
  flyway:
    enabled: true
    locations: classpath:db/migration
    table: flyway_history_client_manager
    baseline-on-migrate: true
    baseline-version: 0

  cloud:
    openfeign:
      client:
        config:
          # Card info is optional in GET /customers/{id}: fail fast and answer without it.
          card-processor:
            connect-timeout: 1000
            read-timeout: 2000
    aws:
      region:
        static: ${AWS_REGION:us-east-1}
      credentials:
        access-key: ${AWS_ACCESS_KEY_ID:test}
        secret-key: ${AWS_SECRET_ACCESS_KEY:test}
      sqs:
        endpoint: ${AWS_SQS_ENDPOINT:http://localhost:4566}

# Base URLs of the services we call. Tests replace them with an in-process WireMock.
integrations:
  card-processor:
    base-url: ${CARD_PROCESSOR_BASE_URL:http://localhost:8083}

app:
  customer:
    # Minimum age to register a customer. 0 disables the check: the rule is implemented and tested, but the
    # actual minimum age varies (product, regulation) and isn't confirmed yet, so it is not enforced.
    minimum-age: ${CUSTOMER_MINIMUM_AGE:0}
  security:
    jwt:
      # HS256 signing key, at least 32 bytes. The default is for local development only:
      # always set JWT_SECRET anywhere else.
      secret: ${JWT_SECRET:local-dev-only-secret-change-me-0123456789}
      expiration: ${JWT_EXPIRATION:1h}
      issuer: rpe-client-manager
    # Created on startup (as ADMIN) if no user with this username exists. Leave the username empty to skip.
    bootstrap-user:
      username: ${AUTH_BOOTSTRAP_USERNAME:admin}
      password: ${AUTH_BOOTSTRAP_PASSWORD:admin12345}
  sqs:
    # Published by rpe_client_manager, consumed by rpe_card_processor.
    client-manager-queue: ${CLIENT_MANAGER_QUEUE:rpe-client-manager-queue}
    # How long the outbox relay waits for SQS to accept one event before counting the attempt as failed.
    send-timeout: ${SQS_SEND_TIMEOUT:5s}
  # Transactional outbox for card production requests (see OutboxRelay).
  outbox:
    poll-interval: ${OUTBOX_POLL_INTERVAL:1s}
    batch-size: 50
    # Backoff doubles from initial-backoff up to max-backoff; after max-attempts the event is FAILED (about 23 min).
    max-attempts: 12
    initial-backoff: 2s
    max-backoff: 5m
    # SENT events (payload already cleared) are deleted after this long, daily at 03:00.
    retention: 7d
    purge-cron: "0 0 3 * * *"
    # AES-256 key (base64 of 32 bytes) encrypting pending payloads (they carry name, CPF and credit info).
    # The default is for local development only: always set OUTBOX_ENCRYPTION_KEY anywhere else.
    encryption-key: ${OUTBOX_ENCRYPTION_KEY:QmVp3Z1q7j5V0kqKp2XN4r8sYwT6uH9aLcE1dF3gJ0M=}

management:
  endpoints:
    web:
      exposure:
        include: health,info
